###############
# CONFIGURATION
###############

source 'https://rubygems.org'

ruby '2.7.2'

# Font Metrics Parser for the Prawn PDF generator.
gem 'ttfunk'

# Prawn is a fast, tiny, and nimble PDF generator for Ruby.
# This fork tracks latest Prawn and is compatible with templates gem below.
gem 'prawn'

# A extention to prawn that allows to include other pdfs either as background to
# write upon or to combine several pdf documents into one.
gem 'prawn-templates'